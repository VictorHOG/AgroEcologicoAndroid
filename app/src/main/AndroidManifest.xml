<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agroecologico">
    <!-- <uses-permission android:name="android.permission.CAMERA"/> -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgroEcologico">
        <activity android:name=".ShopperOrderActivity"></activity>
        <activity android:name=".OrderDetailsVendorActivity" />
        <activity android:name=".ProductDetailActivity" />
        <activity android:name=".StallDetailActivity" />
        <activity android:name=".VendorAddStallImage" />
        <activity android:name=".VendorAddWorker" />
        <activity android:name=".VendorAddProduct" />
        <activity android:name=".AdminCreateStallActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".ShopperActivity" />
        <activity android:name=".VendorActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>